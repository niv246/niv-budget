[phases.setup]
nixPkgs = ["...", "openssl"]

[phases.install]
cmds = ["npm install"]

[phases.build]
cmds = ["npx prisma generate", "npx tsc"]

[start]
cmd = "npx prisma migrate deploy && node dist/index.js"
